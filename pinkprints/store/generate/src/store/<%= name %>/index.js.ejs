/* */
// Types
export const ACTION = '<%= name %>/ACTION';

/* */
// Action Creators

/* */
// Reducer
const initialState = {};

export default (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
