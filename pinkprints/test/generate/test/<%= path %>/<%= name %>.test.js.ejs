import { expect } from 'chai';
import {
<% exports.forEach(e => { -%>
    <%= e %>,
<% }) -%>
} from '@<%= path %>/<%= name %>';

describe('<%= name %>', () => {
<% exports.forEach(e => { -%>

    describe('<%= e %>', () => {
        it('works', () => {
            expect(true).to.eq(false);
        });
    });
<% }) -%>

});
